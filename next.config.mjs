/** @type {import('next').NextConfig} */
const nextConfig = {
  //   experimental: {
  //     useLightningcss: true,
  //   },
  images: {
    domains: ["images.unsplash.com"],
  },
};

export default nextConfig;
